# Copy to .env.local for development (gitignored) and point it at your
# running server/ instance. For production, set this in your GitHub Actions
# workflow / hosting config to your deployed backend's URL.
VITE_API_URL=http://localhost:3001
